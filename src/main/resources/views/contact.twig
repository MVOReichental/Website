<div class="page-header">
	<h1>Kontakt</h1>
</div>

{% for contact in json %}
	<address>
		<strong>{{ contact.name }}</strong><br/>
		<span class="pre">{{ contact.text }}</span>

		{% if contact.mailbox %}
			<br/>
			<a class="contact-mail" data-mailbox="{{ contact.mailbox }}"></a>
		{% endif %}
	</address>
{% endfor %}