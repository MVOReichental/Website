<div class="page-header">
	<h1>{{title}}</h1>
</div>

<div class="btn-group hidden-print" id="members-groups">
	{{#groups}}
		{{#active}}
			<a href="{{url}}" class="btn btn-default active">{{title}}</a>
		{{/active}}
		{{^active}}
			<a href="{{url}}" class="btn btn-default">{{title}}</a>
		{{/active}}
	{{/groups}}
</div>

{{{content}}}