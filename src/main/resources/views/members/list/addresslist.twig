<div class="table-responsive" id="members-list">
    <table class="table table-striped table-hover sortable">
        <thead>
            <tr>
                <th data-defaultsort="disabled" class="d-print-none minimum-width"><input type="checkbox" class="members-select-checkbox"/></th>
                <th data-defaultsort="disabled" class="d-print-none minimum-width"></th>
                <th>Vorname</th>
                <th>Nachname</th>
                <th>Email</th>
                <th data-defaultsort="disabled">Telefon</th>
            </tr>
        </thead>
        <tbody>
            {% for user in users %}
                <tr data-id="{{ user.id }}">
                    <td class="d-print-none minimum-width"><input type="checkbox" class="members-select-checkbox"/></td>
                    <td class="d-print-none minimum-width"><img src="users/{{ user.id }}/profile-picture.jpg?hash={{ user.profilePictureHash }}" height="40" loading="lazy"/></td>
                    <td class="first-name">
                        <a href="internal/members/{{ user.username }}" class="d-print-none"><i class="fas fa-info-circle"></i></a>
                        {{ user.firstName }}
                    </td>
                    <td class="last-name">{{ user.lastName }}</td>
                    <td>{{ user.email }}</td>
                    <td>
                        {% for contact in user.contacts %}
                            <div>{{ contact.title|raw }}: {{ contact.value }}</div>
                        {% endfor %}
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div>